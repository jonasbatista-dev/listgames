import { Meta, StoryObj } from '@storybook/react';
import Heading from '.';

export default {
  title: 'Heading',
  component: Heading,
  tags: ['autodocs'],
} as Meta;

export const Default: StoryObj = {};
